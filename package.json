{
  "name": "next-release-tag",
  "version": "1.0.0",
  "description": "Github Action to generate next Release Tag",
  "author": "amitsingh-007",
  "license": "MIT-open-group",
  "homepage": "https://github.com/amitsingh-007/next-release-tag#readme",
  "scripts": {
    "build": "ncc build src/index.js --out dist --target es12",
    "clean": "rm -r dist",
    "dist": "npm run eslint && npm test && npm run clean && npm run build",
    "eslint": "eslint .",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "1.9.0",
    "@actions/github": "5.0.3"
  },
  "devDependencies": {
    "@babel/preset-env": "7.18.9",
    "@vercel/ncc": "0.34.0",
    "eslint": "8.20.0",
    "jest": "28.1.3"
  }
}
